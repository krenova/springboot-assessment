# Use a different database for testing
spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}_test?createDatabaseIfNotExist=true

# Re-create the schema for every test run
spring.jpa.hibernate.ddl-auto=create-drop

# Optional: keep SQL logging for testing
spring.jpa.show-sql=true
